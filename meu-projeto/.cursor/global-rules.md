# AIOS - Regras globais (opcional para User Rules)

Se quiser que o Cursor aplique as regras do AIOS em todos os projetos, copie o conteúdo abaixo em **Settings → Cursor Settings → Rules for AI → User Rules**.

---

Recognize Synkra AIOS in this workspace:
- Agent activations: @dev, @qa, @architect, @pm, @po, @sm, @analyst
- Commands use * prefix: *help, *create-story, *task, *exit
- Work from story files in docs/stories/ when present
- Update story checkboxes [ ] → [x] as tasks complete
- Follow agent-specific workflows when user invokes an agent
